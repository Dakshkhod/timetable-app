[phases.setup]
nixPkgs = ["nodejs", "npm"]

[phases.install]
cmds = ["npm ci --only=production"]

[phases.build]
cmds = []

[start]
cmd = "npm start"

# Override any existing build scripts
[phases.preBuild]
cmds = []

[phases.postBuild]
cmds = []
